#include"msp430.h"#include<stdlib.h>#define  MAX_PTS 300// IMPORTANT: PUT YOUR NAME AND ECE BOX NUMBER HERE!!! voidmain(void){     // Give total size (in bytes) of these variables as declared.int   in[MAX_PTS], parabola[MAX_PTS];  //    // At what address is alfa stored?int   alfa = 4;                 //    int   half_max, i;           //    WDTCTL = WDTPW + WDTHOLD;   // Stop watchdog timer    half_max = MAX_PTS/2;       for (i = 0; i < MAX_PTS; i++)   //      {         in[i] = i – half_max;            //        parabola[i]= (in[i]-alfa)*(in[i]-alfa); //    } } 
